# ⚠️ DEPRECATED — this repo is retired

**Rob-AI is a superseded duplicate of the AUBS Knowledge Spine and is archived (read-only).**

- **Canonical Knowledge Spine:** the private repo **`aubs-knowledge`** (`~/aubs-knowledge`), extracted
  2026-07-06 from the live service and richer than this copy (adds `persona_engine/`, `knowledge_api.py`,
  `ui.py`, `welcome.md`).
- **Do not build on this repo.** It is preserved only for history. All code here remains recoverable
  read-only; nothing was deleted.
- **One salvage note:** this repo's `embeddings.py` carries a *Chroma-ONNX offline embedding fallback*
  (`ChromaOnnxEmbedding`) that the canonical service does not yet have — tracked as a port-later
  enhancement in the AUBS OS consolidation plan, so it is not lost.

See `aubs-os/OWNERSHIP_MATRIX.md` (#11, #20) and `aubs-os/docs/RECONCILIATION.md`.

*Truth · Safety · We Got Your Back.*